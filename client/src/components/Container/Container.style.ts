import { styled } from '@mui/material/styles';

export const SContainer = styled('div')(() => ({
	display: 'inline-block',
	borderRadius: '35px',
	backgroundColor: '#fff',
	padding: '26px 20px',
}));
